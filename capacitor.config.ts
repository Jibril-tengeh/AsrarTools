import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asrartools.app',
  appName: 'Asrar Tools',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
